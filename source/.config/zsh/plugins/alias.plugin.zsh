#!/usr/bin/env zsh
# ---------------------------------------------------------------------------------------------------------------------
# MISC ALIASES
#
# A place to define extra aliases that don't fit in another category or make sense to be their own plugin.
# ---------------------------------------------------------------------------------------------------------------------

# Enable wayland backend for Webcord when starting from CLI.
alias webcord='webcord --enable-features=UseOzonePlatform --ozone-platform=wayland'
